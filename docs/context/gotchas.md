# Fallstricke

> Dinge, die überraschend waren oder Zeit gekostet haben. Ein Eintrag hier spart
> dem anderen im Team denselben Abend. Neueste oben.

## Setup

**Flutter-SDK ist auf Frederiks Rechner nicht installiert** (Stand 11.08.2026).
`flutter` und `dart` sind nicht im PATH, kein SDK in den üblichen Pfaden gefunden.
Die `/flutter-*` Commands im Repo funktionieren erst nach der Installation.

---

*Noch keine weiteren Einträge — das Projekt hat noch keinen Code.*
