name: habits
description: >-
  Gewohnheiten, Streaks und die daraus folgenden Charakterwerte fuer Lifes
  Game. Reines Dart, damit sich Streak- und Statkurven durchrechnen lassen,
  ohne die App zu starten (siehe ADR-0008).
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

# Bewusst leer. Ein Kalender und eine Kurve brauchen keine Infrastruktur.
dependencies:

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
