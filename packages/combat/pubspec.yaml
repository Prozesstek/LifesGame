name: combat
description: >-
  Reine Kampflogik fuer Lifes Game. Bewusst ohne Flutter- und Flame-Abhaengigkeit,
  damit Balance in Millisekunden testbar bleibt (siehe ADR-0002).
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

# Bewusst leer. Jede Dependency hier waere eine Verletzung von ADR-0002.
dependencies:

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
